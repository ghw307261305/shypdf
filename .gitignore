node_modules
dist
.claude/
.astro
.wrangler
# 整个目录由 scripts/vendor.mjs 从 node_modules 生成，版本由 package-lock.json 锁定
public/vendor

# 维护者的内部文档（中文：架构、需求、运维、上线清单）。只留在本地，不随仓库公开
docs/

# macOS Finder 目录视图设置，和项目无关
.DS_Store

# 验收测试素材：用 node scripts/make-fixtures.mjs 重新生成
test/fixtures/
test/acceptance/results/
